The ``1-my_list`` module
======================

Using ``my_list``
-------------------
importing module:
    >>> MyList = __import__('1-my_list').MyList

standard usage of print_sorted:
    >>> l = MyList()
    >>> l.append(1)
    >>> l.append(3)
    >>> l.append(2)
    >>> l.append(5)
    >>> l.append(9)
    >>> l.print_sorted()
    [1, 2, 3, 5, 9]
    >>> print(l)
    [1, 3, 2, 5, 9]

negative number usage of print_sorted:
    >>> l = MyList()
    >>> l.append(1)
    >>> l.append(3)
    >>> l.append(-2)
    >>> l.append(5)
    >>> l.append(-9)
    >>> l.print_sorted()
    [-9, -2, 1, 3, 5]
    >>> print(l)
    [1, 3, -2, 5, -9]

repeated element usage of print_sorted:
    >>> l = MyList()
    >>> l.append(1)
    >>> l.append(9)
    >>> l.append(1)
    >>> l.append(5)
    >>> l.append(9)
    >>> l.print_sorted()
    [1, 1, 5, 9, 9]
    >>> print(l)
    [1, 9, 1, 5, 9]
